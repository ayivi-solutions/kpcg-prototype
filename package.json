{
  "name": "kpcg-prototype",
  "version": "18.0.0",
  "private": true,
  "description": "KPCG v18 public prototype: continuous-scroll public root with preserved v17 detailed-record and CMS fallback, deployed with Cloudflare Workers Static Assets.",
  "scripts": {
    "dev": "wrangler dev",
    "social:generate": "node scripts/generate-social-pages.mjs",
    "social:check": "node scripts/generate-social-pages.mjs --check",
    "acceptance": "node scripts/acceptance-smoke.mjs",
    "acceptance:assets": "node scripts/acceptance-smoke.mjs --strict-assets",
    "test": "npm run acceptance",
    "predeploy": "npm run social:check && npm run acceptance",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "verify": "npm run social:check && npm run acceptance && npm run deploy:dry",
    "verify:strict": "npm run social:check && npm run acceptance:assets && npm run deploy:dry"
  },
  "devDependencies": {"wrangler":"^4.0.0"},
  "engines": {"node":">=22"}
}
